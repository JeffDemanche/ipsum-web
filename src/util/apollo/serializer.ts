/**
 * Converts Apollo state into a string to be written to disk.
 */
export const writeApolloState = (): void => {};

/**
 * Load a string from disk into Apollo state.
 */
export const loadApolloState = (file: string): void => {};
