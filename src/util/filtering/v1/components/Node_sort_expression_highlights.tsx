import React from "react";

import { NodeComponent } from "../types";

export const Node_sort_expression_highlights: NodeComponent = ({
  editMode,
  childComponents,
  onAddChild,
  onDeleteSelf,
  onRemoveChild,
}) => {
  return <div>Node_sort_expression_highlights</div>;
};
