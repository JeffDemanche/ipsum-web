import React from "react";

import { NodeComponent } from "../types";

export const Node_predicate: NodeComponent = ({
  editMode,
  childComponents,
  onAddChild,
  onDeleteSelf,
  onRemoveChild,
}) => {
  return <div>Node_predicate</div>;
};
